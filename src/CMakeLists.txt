# add_executable(discretizer discretizer.cpp)


# # Specify the include directories for the executable
# target_include_directories(discretizer PRIVATE ${PROJECT_SOURCE_DIR}/include)

# # Link the ggq_library to the executable
# target_link_libraries(discretizer PRIVATE ggq_library)